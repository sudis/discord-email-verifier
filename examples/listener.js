const { Listener } = require('gcommands');
const settings = require("../config/settings.json");

new Listener({
	name: '',
	event: '',
	run: (client) => {

	}
});

/*

Güzel kardeşim üstteki ile harika eventler oluşturabilirsin.

*/